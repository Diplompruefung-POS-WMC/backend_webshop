package com.example.backend_webshop.strategy;

public class AverageValue implements StatisticStrategy{
    @Override
    public double calculateStatistic() {
        // Example logic for calculating average value
        return 100.0;
    }
}
